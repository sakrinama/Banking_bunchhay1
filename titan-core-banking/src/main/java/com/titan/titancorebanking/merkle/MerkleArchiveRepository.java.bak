package com.titan.titancorebanking.merkle;

import org.springframework.data.jpa.repository.JpaRepository;
import java.util.Optional;

public interface MerkleArchiveRepository extends JpaRepository<MerkleArchiveRecord, Long> {
    Optional<MerkleArchiveRecord> findByMerkleRoot(String merkleRoot);
}
